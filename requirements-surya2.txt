# Shared Surya 2 and Marker 2 extraction runtime. Keep it separate from the
# mlx-vlm environment because this stack requires Transformers 5.
marker-pdf==2.0.0
surya-ocr==0.22.1
torch==2.11.0
torchvision==0.26.0
transformers==5.12.1
pillow==10.4.0
pypdfium2==5.10.1
huggingface-hub==1.5.0
